rootProject.name = 'GearVRf'

include ':Framework'
include ':Sample'

project(':Framework').projectDir = "$rootDir/Framework" as File

new File("$rootDir/Sample").eachDir { dir ->
    if(new File(dir.getPath().concat("/build.gradle")).exists()){
        include(':Sample:' + dir.getName())
    }
}
